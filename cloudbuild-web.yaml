# Cloud Build for VantAIge Next.js
# Substitutions: _WS_URL (wss://...), _SUPABASE_URL, _SUPABASE_ANON_KEY
steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: build
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_WS_URL=${_WS_URL}'
      - '--build-arg'
      - 'NEXT_PUBLIC_SUPABASE_URL=${_SUPABASE_URL}'
      - '--build-arg'
      - 'NEXT_PUBLIC_SUPABASE_ANON_KEY=${_SUPABASE_ANON_KEY}'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/vantaige:${SHORT_SHA}'
      - '.'
  - name: 'gcr.io/cloud-builders/docker'
    id: push
    args:
      - 'push'
      - 'gcr.io/${PROJECT_ID}/vantaige:${SHORT_SHA}'
    waitFor: ['build']
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: deploy
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'vantaige'
      - '--image'
      - 'gcr.io/${PROJECT_ID}/vantaige:${SHORT_SHA}'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
    waitFor: ['push']

substitutions:
  _REGION: us-central1
  _WS_URL: ''
  _SUPABASE_URL: ''
  _SUPABASE_ANON_KEY: ''

images:
  - 'gcr.io/${PROJECT_ID}/vantaige:${SHORT_SHA}'

options:
  logging: CLOUD_LOGGING_ONLY
